{
  "name": "ragbaz-cc",
  "version": "1.0.0",
  "private": true,
  "description": "ragbaz.cc — static site served from the Cloudflare edge via OpenNext",
  "scripts": {
    "prepare:content": "node scripts/generate-products-page.mjs && node scripts/generate-prospect-pages.mjs && node scripts/build-embedded-docs.mjs && node scripts/ensure-local-mode.mjs && node scripts/sync-public.mjs && node scripts/generate-workspace-index.mjs && node scripts/check-public-leaks.mjs",
    "clean": "node --eval \"import { rmSync } from 'node:fs'; for (const path of ['.next', '.open-next']) rmSync(path, { recursive: true, force: true });\"",
    "dev": "npm run prepare:content && next dev",
    "build": "npm run prepare:content && next build",
    "cf:build": "npm run clean && npm run build && opennextjs-cloudflare build --skipNextBuild",
    "preview": "npm run cf:build && opennextjs-cloudflare preview",
    "deploy": "npm run cf:build && opennextjs-cloudflare deploy --env=\"\" && npm run graphql:refresh",
    "staging:env": "sh scripts/bootstrap-staging-env.sh",
    "staging:serve": "sh scripts/start-staging.sh",
    "cf-typegen": "wrangler types",
    "graphql:refresh": "node scripts/refresh-workspace-index.mjs",
    "graphql:push": "node scripts/push-workspace-index.mjs",
    "hooks:install": "bash scripts/install-git-hooks.sh",
    "check:public-leaks": "node scripts/check-public-leaks.mjs"
  },
  "dependencies": {
    "graphql": "^17.0.2",
    "next": "^15.4.6",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "smol-toml": "^1.7.0"
  },
  "devDependencies": {
    "@opennextjs/cloudflare": "^1.8.0",
    "wrangler": "^4.107.0"
  }
}
